plugins {
    id 'java'
    id 'org.springframework.boot' version '3.4.0-SNAPSHOT'
    id 'io.spring.dependency-management' version '1.1.6'
}

dependencies {
    implementation project(':common')
}

bootJar {
    mainClass = 'bokovuruskhan.roles_rest.RolesRestApplication'
}